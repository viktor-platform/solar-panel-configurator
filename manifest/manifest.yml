version: '1'

# Define all entity types
entity_types:
  Configurator:
    has_designer: true
    show_properties: false

# On app start, create sample entity
entities:
- entity_type: Configurator
  properties:
    name: Configurators